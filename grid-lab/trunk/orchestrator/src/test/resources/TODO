local beans

factory beans

support for generics

deploy with composite interface (PlatformBean) to avoid cast to PlatformBean

scenario daemon for death nodes detection

when method invoked log scope reductions caused by arguments

track status either bean deployed or not
